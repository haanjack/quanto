# Requirements for AMD ROCm environment
# Note: PyTorch should be installed from ROCm-enabled wheel first
# Install with: pip install -r requirements-rocm.txt

# PyTorch with ROCm support (install this first if not in Docker)
# pip install torch --index-url https://download.pytorch.org/whl/rocm6.0

# Core dependencies
transformers>=4.40.0
accelerate>=0.30.0
datasets>=2.18.0
safetensors>=0.4.0

# Quark and quantization dependencies
onnx>=1.16.0
onnxslim>=0.1.0
onnxscript>=0.1.0

# Tokenization
sentencepiece>=0.2.0
tiktoken>=0.6.0
zstandard>=0.22.0

# Evaluation dependencies
nltk>=3.8.0
lm-eval>=0.4.0
evaluate>=0.4.0
scikit-learn>=1.4.0

# vLLM for ROCm (optional - may require ROCm 6.0+)
# Install from: pip install https://vllm-wheels.s3.us-west-2.amazonaws.com/rocm/vllm-1.0.0.dev-cp38-abi3-manylinux1_x86_64.whl
# Or build from source for ROCm support

# Utilities
tqdm>=4.66.0
rich>=13.0.0
pyyaml>=6.0
pydantic>=2.0.0
psutil>=5.9.0

# Note: Quark should be installed from source:
# pip install /path/to/quark
